language: python
python:
  - "3.4"
  - "3.5"
  - "3.6"
  - "3.7"
# command to install dependencies
install:
  - pip install pytest
  - pip install pytest-mock
  - pip install -e .
# command to run tests
script:
  - pytest tests
